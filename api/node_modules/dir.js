var path = require('path');

module.exports = {
	basedir: process.env.PWD,
	models: path.join(process.env.PWD, '/models'),
	connection: path.join(process.env.PWD, '/configs/connection.js'),
	orm: path.join(process.env.PWD, '/configs/orm.js'),
	routes: path.join(process.env.PWD, '/routes'),
	helpers: path.join(process.env.PWD, '/helpers.js')
};